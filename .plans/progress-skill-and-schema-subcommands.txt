================================================================================
TASK-001: Restructure clap CLI to support subcommands alongside positional query arg
Date: 2026-04-10T12:30:00Z
================================================================================

Summary:
Refactored Cli struct to support subcommands (schema, skill) while preserving
positional query arg behavior. Extracted ConnectionArgs, created QueryArgs,
added Commands enum. Removed .schema magic string handling.

Files Changed:
- src/main.rs (modified: extracted ConnectionArgs/QueryArgs structs, added Commands enum, split run() into run_query_mode()/run_schema_mode(), removed .schema magic)
- tests/integration.rs (modified: schema test uses "schema" subcommand instead of ".schema" arg)
- bench/token_count.rs (modified: auto-formatted by cargo fmt)

Key Decisions:
- Skill variant is a placeholder that exits with error until task-005 wires it up
- ConnectionArgs derives Clone for flexibility
- args_conflicts_with_subcommands ensures clean separation between query mode and subcommands

Next Task: task-002

================================================================================
TASK-002: Create src/skill.rs with agent detection and canonical path resolution
Date: 2026-04-10T13:00:00Z
================================================================================

Summary:
Created src/skill.rs with 11-agent registry, path expansion (~/,$XDG_CONFIG_HOME),
detect_agents(), canonical_dir(), find_agent(), all_agents(). No new deps.

Files Changed:
- src/skill.rs (created: agent registry, detection, path resolution)
- src/main.rs (modified: added mod skill declaration)

Key Decisions:
- Used std::env::var("HOME") instead of dirs crate to avoid new dependency
- Agent struct fields detect_dir/skills_dir are private, exposed via methods
- find_agent() added for --agent flag filtering in future tasks

Next Task: task-003

================================================================================
TASK-003: Implement skill install with symlink and copy fallback
Date: 2026-04-10T13:30:00Z
================================================================================

Summary:
Added install() to src/skill.rs: embeds SKILL.md via include_str!, writes to
canonical dir, symlinks to each detected agent's skills dir with copy fallback.
Created skills/neo4j-query/SKILL.md as the embed source (copied from .claude/).

Files Changed:
- src/skill.rs (modified: added install(), copy_dir(), remove_any(), SKILL_MD const)
- skills/neo4j-query/SKILL.md (created: compile-time embed source for include_str!)

Key Decisions:
- Created skills/neo4j-query/SKILL.md early (task-006 dep) so include_str! compiles
- Used #[cfg(unix)] for symlink, non-unix always falls back to copy
- remove_any() uses symlink_metadata to correctly handle symlinks vs dirs

Next Task: task-004

================================================================================
TASK-004: Implement skill remove and skill list subcommands
Date: 2026-04-10T14:00:00Z
================================================================================

Summary:
Added remove() and list() functions to src/skill.rs. remove() deletes skill
symlinks/dirs from detected agents with optional --agent filter, cleans up
canonical dir when no agents remain. list() prints all 11 agents with
detected/installed status in a formatted table.

Files Changed:
- src/skill.rs (modified: added is_skill_installed(), remove(), list())

Key Decisions:
- is_skill_installed() uses symlink_metadata to detect both symlinks and dirs
- remove() cleans up canonical dir and empty parent dirs when no agents have skill
- list() always shows all 11 agents regardless of detection status

Next Task: task-005
