================================================================================
TASK-001: Emit database block (neo4jVersion, edition, defaultCypherVersion) in run_schema
Date: 2026-04-20T00:00:00Z
================================================================================

Summary:
Extended run_schema in src/main.rs w/ 2 extra run_cypher calls. (1)
`CALL dbms.components()` -> neo4jVersion (first of versions[]) + edition.
(2) `SHOW SETTINGS WHERE name = 'db.query.default_language_version'` ->
defaultCypherVersion, coerced to String, fallback "5" on Err or zero rows
(Neo4j 5.x). Errors from either query swallowed — schema still emits partial
data. Final JSON built via explicit Map::insert in order (database first,
then nodes/relationships/indexes/constraints) — preserve_order is enabled
transitively via toon-format so insertion order is kept in TOON output.

Files Changed:
- src/main.rs (modified: added step 6 + restructured final Ok to ordered Map)
- .plans/tasks-schema-database-version-info.yml (task-001 marked completed)

Key Decisions:
- Reused insert_if_present for neo4jVersion/edition to elide nulls/empties.
- defaultCypherVersion unconditionally inserted (always a String per REQ-F-006).
- `database` map only attached if non-empty — matches PRD wording.
- Confirmed serde_json `preserve_order` active via toon-format dep (cargo tree).

Feedback:
- cargo build: clean
- cargo test: 75 passed, 0 failed
- cargo clippy: clean
- cargo fmt --check: clean

Next Task: task-002
